services:
  stlfiles:
    image: stlfiles/backend:latest
    container_name: stlfiles_backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${STLFILES_PORT:-8321}:8321"
    volumes:
      - ${STLFILES_DATA:-./data}:/app/data
    restart: unless-stopped
